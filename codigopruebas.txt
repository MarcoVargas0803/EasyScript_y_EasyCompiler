INICIO {
    IMPRIMIR("Iniciando cuenta regresiva:");

    // Ejemplo de un ciclo que se repite 5 veces
    PARA (ENT I = 5; I > 0; I--) {
        IMPRIMIR("Faltan " + I + " segundos...");
    }
    
    IMPRIMIR("¡Despegue!");
} FIN
